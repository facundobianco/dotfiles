# windows
fontname        "dina:size=09pt"
borderwidth     0
gap             16 0 0 0
ignore          Xfce4-notifyd

# unmap
bind C-Left     unmap
bind C-Right    unmap
bind C-Down     unmap
bind C-Up       unmap
bind C-slash    unmap
bind CS-Left    unmap
bind CS-Right   unmap
bind CS-Down    unmap
bind CS-Up      unmap
bind M-Left     unmap
bind M-Right    unmap
bind M-Down     unmap
bind M-Up       unmap
bind M-h        unmap
bind M-j        unmap
bind M-k        unmap
bind M-l        unmap
bind M-H        unmap
bind M-J        unmap
bind M-K        unmap
bind M-L        unmap
bind M-period   unmap
bind M-Tab      unmap
bind M-Return   unmap
bind M-slash    unmap
bind C-slash    unmap
mousebind "1"   unmap
mousebind "2"   unmap
mousebind "3"   unmap
mousebind M-2   unmap

# bind
bind 4-q        delete
bind 4S-q       quit
bind 4-r        restart
bind 4-k        lock
bind M-F2       exec
bind 4-slash    search
mousebind 4-1   window_resize
# Uncomment if you use
# OpenBSD:
# bind XF86AudioLowerVolume "mixerctl outputs.master=-5"
# bind XF86AudioRaiseVolume "mixerctl outputs.master=+5"
# bind XF86AudioMute        "mixerctl outputs.master=0"
# Slackware:
# bind XF86AudioLowerVolume "amixer --quiet set Master 3-"
# bind XF86AudioRaiseVolume "amixer --quiet set Master 3+"
# bind XF86AudioMute        "amixer --quiet set Master toggle"


# bind4apps
bind 4-u        urxvt
bind 4-l        leafpad
bind 4-f        firefox
bind 4-c        "urxvt -name casa -e ssh casa"

# Group 1
autogroup 1     "main,URxvt"
# Group 2
autogroup 2     Firefox
# Group 3
autogroup 3     "casa,URxvt"
# Group 4
autogroup 4     "urxvt,URxvt"
autogroup 4     XTerm
autogroup 4     Skype
autogroup 4     Linphone
# Group 5 
autogroup 5     libreoffice-startcenter
autogroup 5     libreoffice-writer
autogroup 5     libreoffice-calc
autogroup 5     libreoffice-draw
autogroup 5     Soffice
autogroup 5     Zathura
autogroup 5     Leafpad
autogroup 5     feh
# Group 6
autogroup 6     Gtkpod
autogroup 6     Kicad
autogroup 6     Eeschema
autogroup 6     Cvpcb
autogroup 6     Pcbnew
autogroup 6     Gerbview
autogroup 6     Gimp
autogroup 6     MPlayer
autogroup 6     Chirpw
# Group 7
autogroup 7     rdesktop

# select groups
bind 4-Left     rcyclegroup
bind 4-Right    cyclegroup
bind 4-Up       rcycleingroup
bind 4-Down     cycleingroup
bind 4-0        nogroup
bind 4-1        grouponly1
bind 4-2        grouponly2
bind 4-3        grouponly3
bind 4-4        grouponly4
bind 4-5        grouponly5
bind 4-6        grouponly6
bind 4S-1       movetogroup1
bind 4S-2       movetogroup2
bind 4S-3       movetogroup3
bind 4S-4       movetogroup4
bind 4S-5       movetogroup5
bind 4S-6       movetogroup6
