[user]
       	name = Facundo Bianco
	email = vando at van dot do
[core]
	editor = jed
	excludesfile = .gitignore
[push]
	default = matching
[alias]
        aa = add -A
        br = branch
	co = checkout
	ci = commit -a --allow-empty-message --no-edit
        go = !git ci && git push
	ls = ls-files
	st = status -s
	up = pull
