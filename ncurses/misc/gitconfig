[user]
	email = vando@van.do
	name = Facundo Bianco
[core]
	editor = jed
	excludesfile = .gitignore
	pager = less -R
[push]
	default = matching
[alias]
        aa = add -A
        br = branch
	co = checkout
	ci = commit -a --allow-empty-message --no-edit
        go = !git ci && git push 
	ls = ls-files
	st = status -s
	up = pull
[color]
	ui = false
	status = true
