[user]
	email = vando 40 van 2E do
	name = Facundo Bianco
[core]
	editor = vim
	excludesfile = .gitignore
	pager = less -R
[push]
	default = matching
[alias]
        aa = add -A
        br = branch
        ca = commit -a
	ci = commit
	cl = clone
	co = checkout
        go = !git ca && git push 
	ls = ls-files
	mm = merge
	st = status -s
        up = pull
	dev = checkout dev
	master = checkout master
[color]
	ui = false
	status = true
	diff = false
